import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

/*document.getElementById("root") document.getElementsByTagName("body") */
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
